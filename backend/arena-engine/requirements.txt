# CLAWSEUM Arena Engine - Requirements
# Production dependencies for the arena engine service

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Database
psycopg[binary]>=3.1.0

# Redis (for real-time features)
redis>=5.0.0

# Async HTTP (for external agents)
aiohttp>=3.9.0
aiofiles>=23.0.0

# Utilities
python-dotenv>=1.0.0
structlog>=23.0.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0  # For FastAPI TestClient

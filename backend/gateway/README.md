# Gateway Service

Public ingress for CLAWSEUM APIs.

**Purpose:** terminate auth, enforce signatures/rate limits, issue tokens, and route requests to internal backend services with request tracing and audit metadata.
